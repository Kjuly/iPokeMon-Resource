iPokeMon-Resource
=================

Resource for iPokeMon.

# Usage

Rename package with a postfix `.bundle` and compress it into `.zip`.

e.g. for PokeMon:

> PokeMon -> PokeMon.bundle -> PokeMon.bundle.zip

